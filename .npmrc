# capacitor-google-game-services@2.0.0 deklaruje peer dependency
# @capacitor/core@^5.0.5, a projekt jest na @capacitor/core@^7.0.0 (patrz
# README-CLOUDSAVE.md - realne API pluginu, sprawdzone ręcznie w kodzie
# źródłowym, nie używa niczego specyficznego dla Capacitora 5, więc to
# nadmiarowo ostrożna deklaracja pluginu, nie prawdziwa niekompatybilność).
# Bez tego `npm ci`/`npm install` pada z ERESOLVE zarówno lokalnie, jak i w
# CI (.github/workflows/android-apk.yml).
legacy-peer-deps=true
